// Shared content-width formula for the authenticated/dashboard screens, so
// every one of them agrees on what "centered column" means instead of each
// faking it with its own ml/pl percentage.
export const DASHBOARD_CONTAINER = "mx-auto w-full max-w-4xl px-4 sm:px-6 lg:px-8";
